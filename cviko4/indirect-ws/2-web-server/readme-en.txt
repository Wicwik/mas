Compile the simple webserver, which responds to any HTTP request by the content of the web page
with salutation Hi! (Ahoj!)

Its code is based on the HTTP header got from the previous part of exercise  
just the length of HTML code has been changed

launch the webserver and in browser open http://localhost:80

change the port to 7171 (80 which is standard port for http is not allowed by the firewall)
browse to e.g. http://158.195.28.167:7171 where the IP je address of your classmate
(work in couples)
